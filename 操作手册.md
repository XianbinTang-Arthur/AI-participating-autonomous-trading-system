# AI Autonomous Trading System 操作手册

本手册面向当前仓库的实际运行状态，重点说明如何启动、如何切换模式、如何查看运行状态、如何做 OKX 模拟盘测试，以及出现问题时该去哪里排查。

当前仓库定位：
- 支持本地演示模式
- 支持真实 OKX 行情 + 本地纸面交易
- 支持受严格保护的 OKX 模拟盘真实下单
- 不支持真实资金实盘交易

## 1. 目录说明

项目核心目录：
- `aats/`：核心业务代码、服务、Schema、运行时装配
- `apps/`：各服务启动入口
- `configs/`：配置模板
- `scripts/`：本地脚本、回放脚本
- `tests/`：测试
- `logs/`：运行日志目录
- `docs/`：任务文档、设计记录

常用根目录文件：
- `README.md`：英文主说明
- `.env.example`：环境变量模板
- `.env`：你的本地实际配置
- `操作手册.md`：本中文操作手册

## 2. 当前支持的运行模式

### 2.1 `local_demo`

用途：
- 本地演示
- 不访问交易所
- 本地 market publisher + paper execution

特点：
- 最安全
- 不会提交任何交易所订单
- 适合功能验证和本地开发

### 2.2 `real_market_paper`

用途：
- 接真实 OKX 行情
- 读取 OKX 账户状态
- 但执行仍然走本地 paper fill

特点：
- 有真实市场数据
- 有真实账户只读同步
- 不会向 OKX 提交订单

### 2.3 `guarded_simulated_submit_dry_run`

用途：
- 走 OKX 执行链路
- 构造真实下单 payload
- 但提交仍然被阻断

特点：
- 用于验证 submit 路径、payload、gating
- 不会向 OKX 发真实模拟盘订单

### 2.4 `guarded_simulated_submit_enabled`

用途：
- 在严格安全门槛全部通过时
- 向 OKX 模拟盘真实提交订单

特点：
- 当前仓库唯一允许交易所侧真实 submit 的模式
- 仅允许 OKX 模拟盘
- 默认仍然是阻断的，必须显式打开相关配置

### 2.5 `guarded_live_blocked` / `guarded_live_enabled`

用途：
- 仅保留未来真实资金实盘的模式骨架

特点：
- 当前仓库不支持真实资金实盘
- 即使切到这些 profile，真实资金 live trading 仍然会被策略/模式/adapter 阻断

## 3. 安装与初始化

### 3.1 创建虚拟环境并安装

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e .
```

### 3.2 PostgreSQL 持久化可选初始化

如果你要启用 PostgreSQL：

```powershell
psql postgresql://aats:aats@localhost:5432/aats -f migrations/0001_postgres_storage.sql
psql postgresql://aats:aats@localhost:5432/aats -f migrations/0002_execution_and_audit_correlation.sql
psql postgresql://aats:aats@localhost:5432/aats -f migrations/0003_audit_execution_plan_refs.sql
```

如果只是本地功能验证，可以先用内存模式，不强制要求 PostgreSQL。

## 4. 环境配置

### 4.1 基本原则

配置来源有两层：
- `configs/*.yaml`：项目默认 profile 配置
- `.env`：你的本机实际覆盖配置

建议做法：
- 不要直接在代码里改硬编码参数
- 复制 `.env.example` 为 `.env`
- 所有本机账号、密钥、调试参数都放 `.env`

### 4.2 最常用环境变量

模式与运行方式：

```env
AATS_CONFIG_PROFILE=real_market_paper
AATS_MODE=paper_live
AATS_MARKET_DATA_BACKEND=okx
AATS_EXECUTION_BACKEND=paper
AATS_ACCOUNT_BACKEND=okx
AATS_ACCOUNT_READ_ENABLED=true
```

OKX 全球站默认端点：

```env
AATS_OKX_REST_URL=https://www.okx.com
AATS_OKX_PUBLIC_WS_URL=wss://ws.okx.com:8443/ws/v5/public
AATS_OKX_BUSINESS_WS_URL=wss://ws.okx.com:8443/ws/v5/business
```

OKX 账户密钥：

```env
AATS_OKX_API_KEY=
AATS_OKX_API_SECRET=
AATS_OKX_API_PASSPHRASE=
```

日志配置：

```env
AATS_LOG_LEVEL=INFO
AATS_LOG_DIR=logs
AATS_LOG_ROTATE_MAX_BYTES=5242880
AATS_LOG_BACKUP_COUNT=7
```

Operator API 鉴权：

```env
AATS_OPERATOR_AUTH_ENABLED=false
AATS_OPERATOR_READ_API_KEY=
AATS_OPERATOR_WRITE_API_KEY=
```

### 4.3 OKX 模拟盘真实 submit 所需最小配置

如果你要做 OKX 模拟盘真实 submit 测试，建议至少这样配置：

```env
AATS_CONFIG_PROFILE=guarded_simulated_submit_enabled

AATS_MODE=guarded_live
AATS_MARKET_DATA_BACKEND=okx
AATS_EXECUTION_BACKEND=okx
AATS_ACCOUNT_BACKEND=okx
AATS_ACCOUNT_READ_ENABLED=true

AATS_OKX_SIMULATED_TRADING=true
AATS_LIVE_SUBMIT_ENABLED=true
AATS_GUARDED_EXECUTION_DRY_RUN=false
```

同时强烈建议把风控限制压小：

```env
AATS_ALLOWED_SYMBOLS=["BTC-USDT"]
AATS_MAX_NOTIONAL_PER_SYMBOL=20
AATS_MAX_OPEN_ORDERS=1
AATS_MAX_ABS_POSITION_QTY=0.001
AATS_DEFAULT_ORDER_QTY=0.0001
```

## 5. 启动方式

### 5.1 启动 API 网关

这是最常用的启动方式：

```powershell
.\.venv\Scripts\python.exe -m uvicorn apps.api_gateway.main:app --reload
```

启动后访问：
- 前端控制台：`http://127.0.0.1:8000/`
- UI 别名：`http://127.0.0.1:8000/ui`

### 5.2 启动本地循环

如果只想跑本地 demo loop：

```powershell
.\.venv\Scripts\python.exe scripts/run_local.py --iterations 6 --interval-seconds 0
```

### 5.3 回放与校验

按决策回放：

```powershell
.\.venv\Scripts\python.exe scripts/replay_session.py --decision-id decision_xxx
```

按时间区间回放：

```powershell
.\.venv\Scripts\python.exe scripts/replay_session.py --start-at 2026-03-14T12:00:00Z --end-at 2026-03-14T12:15:00Z
```

## 6. 前端控制台使用说明

前端为轻量静态控制台，无需单独 Node 构建。

当前导航结构：
- `System`
- `Trading`
- `Execution`
- `Diagnostics`

### 6.1 `System`

看什么：
- 系统整体健康状态
- 当前 blockers
- runtime 配置与 cadence
- metrics 概览

适合：
- 先确认系统是不是健康
- 看是不是被 freshness、halt、reconciliation 等条件阻断

### 6.2 `Trading`

看什么：
- portfolio
- balances
- positions
- latest decision
- recent decisions
- replay 状态

适合：
- 看策略有没有产生决策
- 看组合是否更新
- 看风险/决策链是否完整

### 6.3 `Execution`

看什么：
- execution summary
- execution errors
- open orders
- recent fills

适合：
- 看订单有没有发出
- 看 fill 有没有落地
- 看执行链有没有被阻断

### 6.4 `Diagnostics`

看什么：
- latest reconciliation
- mismatch history
- Inspector

适合：
- 看本地状态与交易所状态是否一致
- 看某个 decision / order / fill 的细节链路

### 6.5 顶部操作按钮

- `Refresh now`：立即刷新所有面板
- `Safe Halt`：安全停机，阻断执行
- `Resume`：恢复运行，但会重新检查 blockers 与 freshness
- `Validate Reconciliation`：主动触发一次对账校验

## 7. 关键 API 说明

### 7.1 系统状态

- `GET /system/health`
- `GET /system/mode`
- `GET /system/runtime`
- `GET /system/blockers`
- `GET /system/metrics`
- `GET /system/recovery`

最重要的几个字段：
- `overall_status`
- `runtime_state`
- `operating_state`
- `submit_blocked`
- `submit_blocked_reasons`
- `blockers`

### 7.2 决策相关

- `GET /decision/latest`
- `GET /decision/recent`
- `GET /decision/{decision_id}`
- `GET /risk/latest`
- `GET /policy/latest`
- `GET /audit/{decision_id}`

### 7.3 组合与账户

- `GET /portfolio/latest`
- `GET /portfolio/history`
- `GET /balances`
- `GET /positions`
- `GET /account/state`
- `GET /account/open-orders`
- `GET /account/recent-fills`

### 7.4 执行与成交

- `GET /orders/open`
- `GET /orders/recent`
- `GET /orders/{order_id}`
- `GET /fills/recent`
- `GET /fills/{fill_id}`
- `GET /execution/latest`
- `GET /execution/errors`

### 7.5 对账与回放

- `GET /reconciliation/latest`
- `GET /reconciliation/recent`
- `GET /reconciliation/mismatches`
- `GET /reconciliation/{reconciliation_id}`
- `POST /reconciliation/validate`
- `GET /replay/status`
- `GET /replay/recent-validations`
- `POST /replay/validate/{decision_id}`

## 8. 日志系统

项目现在会自动创建日志目录：

- `logs/runtime/aats.log`
- `logs/debug/debug.log`
- `logs/info/info.log`
- `logs/warning/warning.log`
- `logs/error/error.log`

说明：
- `runtime/aats.log`：全量运行日志
- `debug.log`：仅 DEBUG 级别
- `info.log`：INFO 级别
- `warning.log`：WARNING 级别
- `error.log`：ERROR 及以上

如果你改了 `.env` 中的日志配置，**需要重启服务进程** 才会生效。

## 9. 启动后的标准检查流程

建议每次启动后按这个顺序确认：

1. 打开 `/system/mode`
2. 打开 `/system/health`
3. 打开 `/account/state`
4. 打开 `/portfolio/latest`
5. 打开 `/reconciliation/latest`
6. 打开前端控制台 `/`

你重点看：
- 当前到底是什么 mode
- submit 是不是被 block
- market/account/reconciliation 是否 fresh
- 是否存在 `halt_required`
- execution errors 是否为空

## 10. 常见运行场景

### 10.1 只看系统能否跑通

用：
- `local_demo`

适合：
- 开发联调
- 不连交易所
- 看主链是否完整

### 10.2 看真实行情，但不下任何交易所订单

用：
- `real_market_paper`

适合：
- 看真实行情驱动决策
- 看本地组合变化
- 验证审计、回放、对账链路

### 10.3 验证 OKX 模拟盘 submit 路径但不真的发单

用：
- `guarded_simulated_submit_dry_run`

适合：
- 看 payload
- 看 mode routing
- 看 gating 是否正确

### 10.4 对 OKX 模拟盘做真实 submit 测试

用：
- `guarded_simulated_submit_enabled`

前提：
- 必须是 OKX 模拟盘 key
- 必须 `AATS_OKX_SIMULATED_TRADING=true`
- 必须 `AATS_LIVE_SUBMIT_ENABLED=true`
- 必须 `AATS_GUARDED_EXECUTION_DRY_RUN=false`
- 其他 safety gates 也都要通过

## 11. 常见问题排查

### 11.1 `401 Unauthorized`

优先排查：
- 是否把 REST/WS 端点写成了错误区域
- 是否使用了 OKX 模拟盘 key
- passphrase 是否正确
- 是否启用了 IP 白名单但当前机器 IP 不在白名单内
- 本机时间是否严重不准

全球站通常应使用：

```env
AATS_OKX_REST_URL=https://www.okx.com
AATS_OKX_PUBLIC_WS_URL=wss://ws.okx.com:8443/ws/v5/public
AATS_OKX_BUSINESS_WS_URL=wss://ws.okx.com:8443/ws/v5/business
```

### 11.2 启动后日志很多

通常不是错误，而是 decision trigger 太激进。

重点检查：
- `AATS_DECISION_MIN_INTERVAL_SECONDS_15M`
- `AATS_DECISION_MIN_INTERVAL_SECONDS_1H`
- `AATS_DECISION_MIN_PRICE_MOVE_BPS`
- `AATS_DECISION_MIN_MOMENTUM_DELTA`

如果这些值设得太低，系统会频繁跑 decision cycle。

### 11.3 启动后被阻断

优先看：
- `/system/health`
- `/system/blockers`
- `/reconciliation/latest`

常见阻断原因：
- `market_data_stale`
- `account_state_stale`
- `reconciliation_halt_required`
- `kill_switch_active`
- `live_submit_disabled`
- `guarded_execution_dry_run`

### 11.4 前端正常打开，但部分面板报错

这是允许的降级行为，不会让整个页面白屏。

排查方式：
- 看对应 API 是否返回 401/403/500
- 看 `logs/error/error.log`
- 看 `logs/runtime/aats.log`

### 11.5 重启后进入安全停机态

这通常说明 recovery 发现了这些问题之一：
- 组合重建分歧
- 缺失对账上下文
- 对账上下文过旧
- 恢复到了需要人工审核的 open orders

这时先看：
- `/system/recovery`
- `/reconciliation/latest`
- `/execution/latest`

## 12. 安全边界

当前仓库的安全边界非常明确：

- 不支持真实资金 live trading
- 只支持 OKX Spot
- 只支持当前 MVP symbol universe
- 默认不允许交易所 submit
- 只有 `guarded_simulated_submit_enabled` 才可能向 OKX 模拟盘发单

即使在 `guarded_simulated_submit_enabled` 下，也必须同时满足：
- submit enabled
- simulated trading enabled
- symbol allowlist
- notional 限制
- freshness 正常
- halt=false
- policy/risk 都通过

## 13. 建议的日常操作习惯

建议每次都按这个顺序操作：

1. 先确认 `.env` 与目标模式一致
2. 启动服务
3. 先看 `/system/mode`
4. 再看 `/system/health`
5. 再看 `/account/state`
6. 再看 `/reconciliation/latest`
7. 最后再观察前端控制台和执行/成交页面

如果你要做 OKX 模拟盘真实 submit：

1. 先用 `guarded_simulated_submit_dry_run` 验证
2. 再切到 `guarded_simulated_submit_enabled`
3. 保持 very small 的 notional 和 position 限制
4. 观察 `/orders/open`、`/fills/recent`、`/execution/errors`
5. 测完立即恢复到更安全的模式

## 14. 当前已实现与仍有限制

已实现：
- 真实行情接入
- 纸面交易闭环
- 受保护的 OKX 模拟盘 submit
- 本地组合管理
- 对账、审计、回放
- operator API
- 轻量前端控制台
- 分级日志目录

仍有限制：
- 不支持真实资金实盘
- 不支持多交易所
- 不支持大规模前端管理后台
- 不支持自动修复型 reconciliation
- 不支持完整实盘运维体系

## 15. 参考

建议同时查看：
- `README.md`
- `.env.example`
- `configs/`
- `logs/`
- `docs/`

如果当前系统行为与本手册不一致，应以运行中的 API 返回值和当前仓库代码为准。
