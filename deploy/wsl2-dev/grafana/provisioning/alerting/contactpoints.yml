# =============================================================================
# Grafana Alert Contact Points
#
# Stage 9 策略：只写 Grafana 内部日志 + UI 面板，不接外部通道。
# Stage 10 再接 Slack / email / Telegram webhook。
# =============================================================================

apiVersion: 1

contactPoints:
  - orgId: 1
    name: aats-log-only
    receivers:
      - uid: aats-log-receiver
        type: email
        settings:
          # 占位地址——未配置 SMTP 所以不会真正发送。
          # Grafana 10.4+ 要求 addresses 非空才能通过 provisioning 校验。
          # 告警状态仍然在 UI 面板中追踪。Stage 10 再配置真实 SMTP + 真实收件人。
          addresses: "noreply@aats.local"
        disableResolveMessage: true
