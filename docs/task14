The repository already contains:

- a working operator console with session-aware browser flows
- centralized role-aware operator authorization checks
- API-key compatibility auth for automation and non-browser tooling
- fixed deterministic settings construction that no longer inherits unrelated shell `AATS_*` values when explicit config dicts are passed
- local single-user compatibility behavior for auth-disabled development mode

Now perform the next implementation pass.

Primary goal of this pass:

Replace the current environment-configured browser login account model with a database-backed operator account model stored in PostgreSQL, while preserving session-based browser auth and API-key compatibility auth.

This pass is NOT about:
- changing trading scope
- live real-money trading
- changing exchanges
- changing strategy logic
- replacing the static frontend with a large framework
- removing API-key automation compatibility
- introducing external SSO as a required dependency

This pass IS about:
- database-backed operator identities
- persisted operator accounts
- proper password storage
- browser login against stored operator accounts
- role lifecycle for viewer / operator / admin
- safer long-term auth management than `.env` usernames/passwords

Target behavior of this stage:

Browser Request
-> Login Page
-> Database-backed Credential Verification
-> Session Issuance
-> Role Resolution
-> Operator Console
-> Protected Read / Write API Actions
-> Audited Operator Action

Important constraints:

- Do NOT undo the settings-isolation fix that prevents explicit runtime/test settings from inheriting ambient shell env vars.
- Do NOT break the existing operator session cookie model unless replacing it with an equivalent secure server-side/session-token approach.
- Do NOT remove API-key compatibility for automation.
- Do NOT remove auditability of halt / resume / rebaseline / cancel actions.
- Keep the implementation compatible with the current static frontend approach.
- Keep defaults safe.

Tasks to complete:

1. Introduce persistent operator account storage
- add a PostgreSQL-backed operator account table
- include at minimum:
  - account id
  - username
  - password hash
  - role
  - enabled flag
  - created timestamp
  - updated timestamp
  - last login timestamp
- ensure username uniqueness is enforced

2. Add password hashing
- do not store plaintext passwords in the database
- use a clear password-hash abstraction
- keep verification logic centralized and testable
- make the chosen scheme explicit in code and docs

3. Replace `.env` browser-login credentials with database lookup
- browser login must no longer depend on:
  - `AATS_OPERATOR_VIEWER_USERNAME`
  - `AATS_OPERATOR_VIEWER_PASSWORD`
  - `AATS_OPERATOR_OPERATOR_USERNAME`
  - `AATS_OPERATOR_OPERATOR_PASSWORD`
  - `AATS_OPERATOR_ADMIN_USERNAME`
  - `AATS_OPERATOR_ADMIN_PASSWORD`
- keep session secret and auth-enable flags configurable
- `.env` may still provide a one-time bootstrap path, but not the normal steady-state auth source

4. Keep the three operator roles
- support:
  - viewer
  - operator
  - admin
- preserve the same read/write semantics already established in the current code
- keep role checks centralized and shared across session auth and API-key auth

5. Define bootstrap behavior
- the system should support safe first-run initialization for operator accounts
- choose one explicit bootstrap path, for example:
  - a startup bootstrap admin created from env when the operator table is empty
  - or a dedicated initialization script/command
- bootstrap behavior must be explicit, auditable where practical, and documented
- do not silently recreate deleted privileged accounts on every startup unless explicitly configured

6. Preserve browser login UX
- keep a dedicated login page
- login should verify username/password against the operator account table
- successful login should issue the existing secure session cookie flow
- logout should remain supported
- invalid-session and invalid-credential behavior should remain explicit

7. Preserve API-key compatibility auth
- `X-AATS-API-Key` remains supported for:
  - scripts
  - automation
  - controlled emergency use
- API-key auth must keep flowing through the same authorization layer
- browser usage should continue to prefer login/session auth

8. Extend operator management visibility
Add or improve endpoints/UI visibility for:
- current authenticated identity
- current role
- auth source
- whether DB-backed operator auth is enabled
- whether bootstrap admin mode is active
- whether API-key compatibility is enabled

9. Preserve and extend auditability
- operator action audit records must still capture:
  - actor identity
  - actor role
  - auth source
- login-related flows should record enough metadata for operator traceability without logging secrets
- cancel-order actions must remain audited

10. Database integration requirements
- implement the operator account repository for persistent PostgreSQL storage
- ensure it works with the project’s existing storage/runtime wiring
- migration(s) must be added under `migrations/`
- avoid introducing a second disconnected database access pattern unless there is strong reason

11. Tests
Add meaningful tests for:
- bootstrap account creation or initialization flow
- login success against DB-backed credentials
- login failure against DB-backed credentials
- disabled-account rejection
- role-based read/write authorization
- session flow still working
- API-key compatibility still working
- operator action audit continuity
- no regression in current runtime/recovery/operator flows

Tests must be meaningful.
Do not add shallow tests that only validate route existence.

12. Documentation
Update README and relevant operator/developer docs to explain:
- operator accounts are now database-backed
- how bootstrap initialization works
- how to create the initial admin
- how viewer/operator/admin roles are managed
- browser login/session behavior
- API-key compatibility behavior
- which `.env` auth fields remain relevant
- which old `.env` username/password fields are deprecated or removed

13. Migration and compatibility expectations
- preserve current APIs where practical
- avoid breaking non-browser tooling unnecessarily
- if legacy env-backed login is kept temporarily, mark it clearly as transitional or deprecated
- keep the system operable during migration

Output requirements:

1. First provide the implementation plan for this pass.
2. Then list all files to modify or add.
3. Then implement the changes file by file.
4. After implementation, explicitly summarize:
   - what database-backed auth behavior now exists
   - how bootstrap initialization works
   - what roles and permissions now exist
   - what browser UX changed
   - what API-key compatibility remains
   - what old env-based auth behavior was removed or deprecated
   - what the next engineering pass should focus on

Final rule:

Do not expand trading scope in this pass.
Do not move toward real-money live trading in this pass.
Only implement PostgreSQL-backed operator accounts, login/session auth against stored accounts, and compatible operator authorization/audit behavior.
