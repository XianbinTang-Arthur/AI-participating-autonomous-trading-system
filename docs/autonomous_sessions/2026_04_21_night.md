# 自主迭代会话：2026-04-21 夜场

> **用户状态**：心脏不适，去休息，计划 8 小时后回来审查。
> **我的承诺**：每一个决策都记录在本文件，醒来全部可审。
> **开始时间**：2026-04-21 ~12:00 UTC（北京时间 ~20:00）
> **预期结束**：2026-04-22 ~04:00 UTC（北京时间 ~12:00）

## 铁律（不破底线）
- 绝不触发下单、平仓、转移资金
- 绝不改 `ai_operating_mode` / live/paper 切换
- 绝不改 kill switch / recovery policy 语义字段
- 绝不读凭证文件
- 绝不 deploy 未跑过 regression 的改动
- 遇到高风险/不确定 → 停下、写进本文件等审

## 工作节奏
- 每个 commit 遵循 "假设 / 效果 / 验证 / rollback" 格式
- 每次 deploy 后**实测 before/after**
- 每小时在本文件写一次 checkpoint
- 做完一轮后写"下一步打算做什么 + 为什么"

---

## T+0 上下文（工作起点）

**用户离开前已确认的事实**：
- `5a6c383`（recovery_posture）已 deploy
- `7b30eaf`（health._base_components）刚 deploy 完
- PG idle_in_tx 60s 安全网 (`afddc1a`) 已生效
- UI 卡顿根因：reconciliation_reports 全表扫（两个调用点都改了）
- Full-scan monitor 04 分钟内 0 次：`t=30s..240s fullscan_queries=0`

**待办 backlog（按价值排序）**：

### Tier 1 紧急
- [ ] 验证 `7b30eaf` deploy 后 full-scan 彻底消失（15 分钟稳态观察）
- [ ] P2-2 `_cached_ttl` 惊群修复
- [ ] `parallel_fetch` 降并发（shared 12→8, inner 4→2）
- [ ] 建立 recovery_view / dashboard_bundle P50/P95/P99 基线

### Tier 2 系统扫描
- [ ] grep 所有 `select(XXXModel)` 无 WHERE 的 query
- [ ] 审查所有 `_cached_ttl` / `_cached` 是否有惊群风险
- [ ] 扫 idle-in-tx 之外的 session lifecycle 问题
- [ ] 扫所有 `repo.history()` 无 scope 调用（可能还有其他 repo）

### Tier 3 业界标杆吸纳
- [ ] SQLAlchemy 2.0 async 岛式迁移 plan（不全改）
- [ ] Grafana OSS singleflight / panel dedup pattern
- [ ] Jane Street "assert thinking" 应用到 risk/recovery
- [ ] Alameda/FTX 教训 → AATS anti-pattern 检查

### Tier 4 自律基础设施
- [ ] `docs/weekly_review/` template
- [ ] `CONTRIBUTING.md` AI agent 工作纪律
- [ ] `scripts/diag/` 生产诊断工具合辑（我这几小时做了 3 个 one-off，值得合并）

### Tier 5 其他欠账
- [ ] P2-2 `gateway_bundle_inner_serial` SOW（原 7 天观察期，生产已证实，可启动）
- [ ] 其他 stale alerts 是否真修好
- [ ] 代码中其他 TODO / FIXME 审查

---

## T+0.5 (UTC ~12:30) · 已完成 full-scan 根治验证

### 完成
- ✅ `5a6c383` recovery_posture history() → latest_for_scope（已 deploy）
- ✅ `7b30eaf` health._base_components history() → latest_for_scope（已 deploy）
- ✅ 5 分钟稳态监控：`fullscan=0 idle_tx=0 worst_slow_wall=none`（T+60/120/180/240/300s）

### Audit agent 系统扫描发现（storage layer 8 个 full-scan 方法）
按运行时 hot path 命中概率评估：
| 方法 | 调用者数 | Hot path 命中 | 决策 |
|------|----------|---------------|------|
| `reconciliation_repo.history()` | 2 (runtime_scope fallback) | Postgres 不走 fallback | 已修（修上游调用者） |
| `event_store.all()` | 1 (replay.py，有 scope/time 守卫) | 非 hot | **暂不动**（低命中） |
| `portfolio_repo.history()` | 4 bootstrap + 1 replay fallback | 只启动时 | **暂不动**（bootstrap 一次性） |
| `fill_outcome_repo.outcomes()` | runtime_scope fallback + replay | Postgres 不走 fallback | 不必改 |
| `exit_execution_repo.list_exit_execution_intents()` | 3 callers | 可能 hot | **待详细审** |
| `execution_repo.order_states()` | 多处 | 多处有 `_for_scope` fallback | **已安全**（recovery_posture 确认） |
| `execution_repo.fills()` | 5 hot callers | AI/portfolio/projection | **待详细审** |
| `operator_repo.all_users()` | 小表 | 非 hot | 忽略 |

### 重要发现：refresh_exit_execution_intents 有潜在 full-scan
- `exit_intent_aggregator.py:726` 调 `execution_repo.order_states()`（全表）
- 调用者 `order_manager._refresh_exit_execution_intents()` 不传 scope
- OrderManager 无 `scope` 属性，要加 scope 需要 constructor 改动
- **决策**：暂不改（中风险，多进程影响面大）。等有量化证据证明这是 hot path bottleneck 再改。

### 不做的（避免过度修）
- 所有 Postgres full-scan 方法本身保留（InMemory 路径还用）
- OrderManager 加 scope 属性（需要慎评）
- asyncpg 迁移（非最优 ROI，用户已知悉）

---

## T+1 (UTC ~12:50) · 发现 & 修复 PG 60s safety net 副作用

### 事故
扫 `scripts/diag/pg_connection_health.sh` 时意外发现：
```
total_advisory_locks = 0  -- 应该是 4！
```

即 afddc1a 的 `idle_in_transaction_session_timeout=60s` 把 4 个 per-process
`single_runtime_lock` 连接**误杀**了。4 个进程 healthy 但单实例保护失效 ——
若启动第二个 gateway 会双跑。

### 根因
`aats/storage/session.py:127-141` acquire_single_runtime_lock：
```python
connection = self.engine.connect()
connection.execute(text("SELECT pg_try_advisory_lock(:k)"), ...).scalar()
# SA 2.0 autobegin tx → connection 处于 idle_in_transaction
```
PG 60s timeout 看到 idle-in-tx → terminate → pg_try_advisory_lock 是
**session-level**（connection close 时释放锁）→ lock 丢失。

### 修复（`23c8e7e`）
acquire 成功后立即 `connection.commit()`：
- connection 状态 idle_in_transaction → idle
- 60s timeout 不对 idle 生效
- advisory_lock 是 session-level（vs xact-level），commit 不释放锁

### 验证
```
advisory_lock_count=4      ← 修复前 = 0
idle_tx_with_advisory=0    ← 修复前 = 4 会积累到 0（被杀）
```
4 locks 正常持有，0 idle-in-tx（不再被 PG 60s 误杀）。

### 教训写进 `docs/weekly_review/` 模板
这是我**没做**的 audit：afddc1a deploy 后我只验证了"gateway 连接 timeout=1min"，
没验证**副作用**——自己的代码里哪些连接本来就是长期 idle-in-tx 设计，
会被误杀。下次：**加全局 safety net 时必须扫一遍仓库所有"长期持连接"的 pattern**。

---

## T+2 (UTC ~13:00) · Tier 4 自律基础设施（`fb70b03`）

### 新建文件
- `CONTRIBUTING.md` — 三档纪律：硬约束、软约束、best practice
- `docs/weekly_review/_template.md` — 每周复盘模板（强制"我做错了什么"一节）
- `docs/autonomous_sessions/` — 独立运行实时日志目录（本文件）
- `scripts/diag/` — 生产诊断工具集
  - `pg_connection_health.sh`
  - `pg_full_scan_audit.sh`
  - `gateway_slow_queries.sh`
  - `recovery_rollback_gap.sh`
  - `panel_latency_histogram.sh`

### 用途
- 审计/追溯：未来 AI agent 或用户能快速看清我做了什么、为什么
- 纠偏：weekly review "我做错了什么" 强制诚实
- 复用：scripts/diag/ 成为生产诊断标准工具箱

---

## T+3 (UTC ~13:30) · Tier 2 审计 + 发现 & 修复 `_cached_ttl` / `_cached` TOCTOU 惊群

### 发现的 bug（存量 latent issue）

`aats/services/operator/query_service.py` 里的 `_cached`（L151-181）和
`_cached_ttl`（L217-282）singleflight 实现有一个 TOCTOU race，在高并发
下会让 singleflight "名存实亡"：

```python
# 旧写法（有 bug）：
try:
    value = loader()
finally:
    with self._cache_lock:
        self._inflight.pop(key, None)
        event.set()          # ← 先唤醒 follower
    # 🔴 race window 从这里开始：锁释放了、cache 还没写
with self._cache_lock:       # ← 再获锁写 cache
    self._ttl_cache[key] = ...
```

时间线：
1. Leader 跑完 loader，进 finally，获锁，`event.set()` 唤醒所有 follower
2. Leader 释放锁（退出第一个 `with` 块）
3. Leader 再去获取第二个 `with` 锁来写 cache
4. 同一刻 follower 也在抢同一个锁来读 cache
5. **如果 follower 抢赢**：读 `_ttl_cache` → 为空 → 回落到 `return loader()`
6. Leader 最后才写 cache，但已经晚了

理论效应：singleflight 本应挡住 N-1 次 loader，实际变成 N 次（惊群放大
1→N+1）。在生产观察到的 parallel_fetch_slow 现象里，这个 race 是
"隐形放大器" —— 根因已解（full-scan），但这个 race 仍让 UI 冷启动 P99
比理论值高。

### 修复（`query_service.py` 内联修复）

把 cache 写入和 `event.set()` 合并到同一个 critical section：

```python
try:
    value = loader()
except BaseException:
    # Leader 失败：唤醒 follower，不写 cache，让 follower 走兜底路径
    with self._cache_lock:
        self._inflight.pop(key, None)
        event.set()
    raise
# Leader 成功：cache 写入 + event.set() 在同一个 lock 里原子发生
with self._cache_lock:
    self._ttl_cache[key] = (...)
    self._inflight.pop(key, None)
    event.set()
return value
```

关键保证：follower 被 `event.set()` 唤醒后，再去拿 `_cache_lock` 时，
**cache 一定已经写入了**（因为 leader 释放锁才能让 follower 获锁，而 leader
释放锁的时候 cache 已在锁里写过了）。

### 确定性测试（`tests/unit/test_cached_ttl_toctou.py`）

用 `_SlowEvent` 子类人为把 `event.set()` 后面加 50ms sleep（只在测试里
替换），把 race 窗口拉到 50ms（生产里只有微秒级）。然后启动 1 leader +
5 follower 抢同 key，断言 loader 仅被调用一次。

3 个测试：
- `test_cached_no_thundering_herd_under_event_set_race` - `_cached` 路径
- `test_cached_ttl_no_thundering_herd_under_event_set_race` - `_cached_ttl` 路径
- `test_cached_ttl_leader_exception_wakes_followers` - leader 抛异常时必须
  唤醒 follower 避免 25s 死等

**结果**：全部通过 ✅。完整单测 2770 passed, 29 skipped, 0 failed, 耗时 6m21s。

### 做了什么、没做什么

做了：
- ✅ 审计 `_cached_ttl` 所有 40+ 个调用点（主要在 operator/query_service、
  operator/account_queries、operator/strategy_queries、operator/report_queries、
  operator/recovery_queries、operator/lifecycle_attribution）
- ✅ 确认 bug 理论上存在 + 写出复现测试 + 修复 + 验证
- ✅ 加锚点测试防止未来 regression

没做（避免过度）：
- ❌ 动 follower 端的 `return loader()` 兜底路径 —— 那条路径是 leader 真正
  失败时的正确 fallback，不能改
- ❌ 把 `_cache_lock` 换成带 fairness 的锁 —— 当前 Python `threading.RLock`
  在大多数场景下足够，不值得为这个引入 queue lock
- ❌ 改动 `_SINGLEFLIGHT_WAIT_SECONDS = 25s` 常量 —— 有 anchor test
  （`test_singleflight_wait_constant.py`）守着

### 教训

这是一个**存量 latent issue**，不是我今天引入的。但仍然记录进
`docs/weekly_review/_template.md` 作为"AI 必须主动审计 latent
concurrency bug"的示例：

> 写代码时的"finally 里先释放信号再写入结果"看似顺理成章，但只要持有
> 结果的状态写入**不在同一个 critical section**，就会造成 TOCTOU race。
> AI 的过去代码审查、包括我自己之前多轮改 query_service.py 都没抓到，
> 只有今天有时间主动做"singleflight 扫一遍"时才找出来。

---

