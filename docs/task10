The repository has already progressed to a guarded simulated-execution stage with:

- real OKX market data working
- OKX demo-account read working
- `real_market_paper` working as a true full-loop paper mode
- guarded simulated submit available for OKX demo trading
- local order state, fill ingestion, portfolio mutation, reconciliation, audit, and replay already present
- operator APIs and runtime controls already present

Now perform the next implementation pass.

Primary goal of this pass:

Introduce account baseline takeover so the system can safely start against a non-empty, previously used account instead of assuming a clean account every time.

This pass is NOT about:
- real-money live trading enablement
- adding new exchanges
- changing the strategy layer
- relaxing existing safety defaults
- redesigning the architecture

This pass IS about:
- startup account takeover
- baseline import
- local state alignment with exchange state
- safe continuation from a known account baseline

Target behavior of this stage:

Startup
-> Read Exchange Account State
-> Build Account Baseline Snapshot
-> Import Local Baseline
-> Restore Portfolio / Reconciliation Starting Point
-> Expose Takeover Status
-> Continue Safe Runtime Operation

Important constraints:

- Do NOT move toward real-money live trading in this pass.
- Do NOT redesign the architecture.
- Do NOT rename major modules, event topics, or service boundaries unless clearly necessary.
- Preserve current `real_market_paper` behavior.
- Preserve guarded simulated submit behavior and safety defaults.
- Keep startup conservative when the imported baseline is incomplete or ambiguous.

Tasks to complete:

1. Introduce exchange account baseline takeover
- on startup, read the current exchange account state:
  - balances
  - positions/holdings where supported
  - open orders
  - recent fills
- build a structured `AccountBaselineSnapshot` or equivalent baseline record
- persist the imported baseline through the existing storage/event model
- keep the baseline auditable and replay-friendly

2. Import local baseline from exchange state
- convert the imported exchange account state into a local portfolio/reconciliation baseline
- do not assume the local state starts from zero balances or zero positions
- support both:
  - clean account startup
  - previously used account startup

3. Distinguish baseline import from normal execution history
- clearly separate:
  - local system-generated execution history
  - imported exchange baseline state
- do not pretend historical exchange state was generated by the current local decision chain
- keep the distinction visible to audit and operator APIs

4. Improve startup recovery status
- expand startup/recovery visibility to include:
  - whether a baseline was imported
  - when it was imported
  - whether the import is safe for automatic continuation
  - whether operator review is required
- keep the recovery state explicit and operator-readable

5. Preserve safe startup behavior
- if baseline import is missing key information, startup must degrade safely
- ambiguous imported state must not be treated as fully trusted execution history
- startup should prefer:
  - `baseline_imported`
  - `baseline_import_requires_review`
  - or equivalent explicit statuses
  instead of silently resuming

6. Improve operator-facing visibility
Add or improve endpoints for:
- current baseline import status
- latest imported baseline timestamp
- imported balance / position summary
- whether current runtime state is based on:
  - cold start
  - imported baseline
  - imported baseline pending review

7. Focused tests
Add practical tests for:
- startup against a clean account
- startup against a non-empty account with balances and fills
- baseline import persistence
- portfolio baseline reconstruction from imported exchange state
- safe degraded startup when imported baseline is incomplete
- no regression in `real_market_paper`
- no regression in guarded simulated submit

Tests must be meaningful and state-focused.
Do not add shallow tests that only validate importability.

8. Documentation
Update README and/or developer/operator docs to explain:
- what baseline takeover means
- when it is used
- how imported exchange state differs from local execution history
- what startup statuses are possible
- what still remains intentionally conservative

Output requirements:

1. First provide the implementation plan for this pass.
2. Then list all files to modify or add.
3. Then implement the changes file by file.
4. After implementation, explicitly summarize:
   - what startup takeover behavior now works
   - what baseline state is imported
   - what remains intentionally conservative
   - what the next engineering pass should focus on

Final rule:

Do not add more product scope in this pass.
Do not move toward real-money live trading in this pass.
Only implement safe account baseline takeover and local baseline import for previously used accounts.
