{
  "workflow": "research_cycle",
  "description": "研究层周期任务，串联数据刷新与完整 Phase 2/3/4/5/6 研究闭环。",
  "schedule_hint": "weekly Sunday 08:00 UTC or on-demand",
  "schedule": {
    "enabled": true,
    "frequency": "weekly",
    "weekday_utc": "SUN",
    "hour_utc": 8,
    "minute_utc": 0
  },
  "tasks": [
    {
      "name": "refresh_recent_data",
      "description": "先运行 data_maintenance，确保研究使用最新数据和最新重建产物。",
      "command": "python scripts/rdp_run_scheduled_workflow.py --workflow data_maintenance",
      "timeout_seconds": 1200,
      "enabled": true,
      "allow_failure": false
    },
    {
      "name": "full_pipeline",
      "description": "一键运行 Phase 2 -> Step 3 -> 导入 -> Phase 3 live attribution -> Phase 4 -> 治理 -> 决策。可由 weekly schedule 自动触发，也可由操作员手动触发；缺少只读 live DB 配置时失败关闭，发布仍需经过审批与 gate。",
      "command": "python scripts/rdp_run_full_pipeline.py --lookback-days 90 --no-stop-on-failure",
      "timeout_seconds": 1800,
      "enabled": true,
      "allow_failure": false
    }
  ]
}
